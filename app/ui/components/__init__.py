"""Reusable Streamlit components.

A component knows how to *render*; it never decides a business rule and
never issues a query of its own. Anything it displays was handed to it by a
page, which got it from a service.
"""

"""Streamlit pages.

Each module exposes a single ``render()``. A page composes components and
calls services; it contains no business rule, no SQL and no arithmetic that
belongs in the domain layer.
"""
